(ns daisy.client.constants)

(def full-frame-width 256)
(def full-frame-height 224)
(def full-frame-size (* full-frame-width full-frame-height 3))
(def buffer-size (* full-frame-width full-frame-height 4))
